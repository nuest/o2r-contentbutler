---
sudo: required
language: node_js
node_js:
  - "6"
  - "7"
  - "8"
services:
  - docker
cache:
  directories:
    - "node_modules"
before_install:
  - docker --version
  - docker pull o2rproject/o2r-loader:latest
  - docker pull mongo:3.4
install:
  - npm install
  - npm install -g mocha
before_script:
  - npm start
  - docker run --name mongodb -d -p 27017:27017 mongo:3.4
  - docker run --name testloader -d -p 8088:8088 --link mongodb:mongodb -e LOADER_MONGO=mongodb://mongodb:27017 o2rproject/o2r-loader:latest
  - sleep 10
script:
  - mocha
after_script:
  #- docker logs mongodb
  - docker logs testloader
